<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= article.title %></title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="container mt-4">
        <h1><%= article.title %></h1>
        <img src="<%= article.imageUrl %>" class="img-fluid" alt="Article Image">
        <p class="mt-3"><%= article.content %></p>
        <p class="text-muted">Published on <%= article.timestamp %></p>

        <h2 class="mt-4">Comments</h2>
        <% article.comments.forEach(comment => { %>
            <div class="card mt-3">
                <div class="card-body">
                    <p class="card-text"><%= comment.text %></p>
                    <p class="card-text text-muted">Comment by <%= comment.user %></p>
                </div>
            </div>
        <% }) %>

        <form action="/comment/<%= article._id %>" method="post" class="mt-4">
            <div class="form-group">
                <label for="comment">Leave a Comment:</label>
                <textarea class="form-control" id="comment" name="comment" rows="3" required></textarea>
            </div>
            <button type="submit" class="btn btn-primary">Submit Comment</button>
        </form>
    </div>
</body>
</html>
